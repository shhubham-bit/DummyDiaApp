import { spacing } from ".";
import { LightScheme } from "./Color/ColorScheme";
import { Typography } from "./Typogrpahy/Typography";

const DiabeticTheme: AppTheme ={
    color: LightScheme,
    typography: Typography,
    spaceing: spacing
}

export {DiabeticTheme}